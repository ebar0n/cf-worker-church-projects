{
  "name": "adventurers",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "verify": "node scripts/check-games.mjs && node scripts/check-text.mjs",
    "migrate": "CI=true wrangler d1 migrations apply church-jordan-projects",
    "dev": "yarn migrate --local && wrangler dev",
    "deploy": "yarn migrate --remote && wrangler deploy"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "wrangler": "^4.112.0"
  },
  "dependencies": {
    "hono": "^4.12.31"
  }
}
